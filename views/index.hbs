<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title> {{ title }} </title>
    </head>

    <body>
        <h2>{{ heading}}</h2>
        <div> {{ text }} </div>
        <ul>
            {{#each projects }}
            <li>
                {{ slug }}: {{ name }}
            </li>
            {{/ each}}
        </ul>
    </body>

</html>